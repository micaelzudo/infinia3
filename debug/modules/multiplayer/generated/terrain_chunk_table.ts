// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import {
  AlgebraicType,
  AlgebraicValue,
  BinaryReader,
  BinaryWriter,
  CallReducerFlags,
  ConnectionId,
  DbConnectionBuilder,
  DbConnectionImpl,
  DbContext,
  ErrorContextInterface,
  Event,
  EventContextInterface,
  Identity,
  ProductType,
  ProductTypeElement,
  ReducerEventContextInterface,
  SubscriptionBuilderImpl,
  SubscriptionEventContextInterface,
  SumType,
  SumTypeVariant,
  TableCache,
  TimeDuration,
  Timestamp,
  deepEqual,
} from "@clockworklabs/spacetimedb-sdk";
import { TerrainChunk } from "./terrain_chunk_type";
import { EventContext, Reducer, RemoteReducers, RemoteTables } from ".";

/**
 * Table handle for the table `terrain_chunk`.
 *
 * Obtain a handle from the [`terrainChunk`] property on [`RemoteTables`],
 * like `ctx.db.terrainChunk`.
 *
 * Users are encouraged not to explicitly reference this type,
 * but to directly chain method calls,
 * like `ctx.db.terrainChunk.on_insert(...)`.
 */
export class TerrainChunkTableHandle {
  tableCache: TableCache<TerrainChunk>;

  constructor(tableCache: TableCache<TerrainChunk>) {
    this.tableCache = tableCache;
  }

  count(): number {
    return this.tableCache.count();
  }

  iter(): Iterable<TerrainChunk> {
    return this.tableCache.iter();
  }
  /**
   * Access to the `chunkKey` unique index on the table `terrain_chunk`,
   * which allows point queries on the field of the same name
   * via the [`TerrainChunkChunkKeyUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.terrainChunk.chunkKey().find(...)`.
   *
   * Get a handle on the `chunkKey` unique index on the table `terrain_chunk`.
   */
  chunkKey = {
    // Find the subscribed row whose `chunkKey` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: string): TerrainChunk | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.chunkKey, col_val)) {
          return row;
        }
      }
    },
  };

  onInsert = (cb: (ctx: EventContext, row: TerrainChunk) => void) => {
    return this.tableCache.onInsert(cb);
  }

  removeOnInsert = (cb: (ctx: EventContext, row: TerrainChunk) => void) => {
    return this.tableCache.removeOnInsert(cb);
  }

  onDelete = (cb: (ctx: EventContext, row: TerrainChunk) => void) => {
    return this.tableCache.onDelete(cb);
  }

  removeOnDelete = (cb: (ctx: EventContext, row: TerrainChunk) => void) => {
    return this.tableCache.removeOnDelete(cb);
  }

  // Updates are only defined for tables with primary keys.
  onUpdate = (cb: (ctx: EventContext, oldRow: TerrainChunk, newRow: TerrainChunk) => void) => {
    return this.tableCache.onUpdate(cb);
  }

  removeOnUpdate = (cb: (ctx: EventContext, onRow: TerrainChunk, newRow: TerrainChunk) => void) => {
    return this.tableCache.removeOnUpdate(cb);
  }}
